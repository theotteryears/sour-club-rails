<% if page.current? %>
  <span class="px-3 py-1 rounded-md bg-indigo-600 text-white"><%= page %></span>
<% else %>
  <%= link_to page, url, remote: remote, rel: page.rel, class: 'px-3 py-1 rounded-md bg-white border border-gray-300 text-gray-700 hover:bg-gray-50' %>
<% end %>
